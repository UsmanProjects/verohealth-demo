package com.veroflow.verohealth.ui.theme

import androidx.compose.ui.graphics.Color

// VeroHealth brand palette — a calm clinical teal/blue rather than a generic
// Material purple, so the app visually reads as "healthcare" at a glance.
val TealPrimary = Color(0xFF00796B)
val TealPrimaryDark = Color(0xFF4DB6AC)
val BluePrimaryContainer = Color(0xFFB2DFDB)
val HealthRed = Color(0xFFD32F2F)     // missed dose / critical
val HealthAmber = Color(0xFFF9A825)   // due soon / warning
val HealthGreen = Color(0xFF2E7D32)   // active / healthy
val HealthGray = Color(0xFF9E9E9E)    // completed / inactive

val LightBackground = Color(0xFFF7FAF9)
val DarkBackground = Color(0xFF101413)
val LightSurface = Color(0xFFFFFFFF)
val DarkSurface = Color(0xFF1B211F)
